import 'package:flutter/material.dart';

class AppLocalizationConstants {
  AppLocalizationConstants._();

  static const Locale fallbackLocale = Locale('en');

  static const List<Locale> supportedLocales = [
    Locale('en'),
    Locale('tr'),
  ];
}
